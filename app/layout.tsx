import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BillVance - Smart Invoicing for Freelancers and Businesses",
  description: "BillVance is a powerful SaaS invoicing platform designed for freelancers and businesses to create, manage, and track invoices effortlessly. Generate professional invoices, automate recurring billing, and integrate seamless payment solutions with Stripe. Simplify your financial workflow with BillVance today.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
